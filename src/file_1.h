// This file includes some very useful functions.
// It may also have some that are less useful.

// This function is better
int truly(bool good);

// Foos the bar
float foo(int bar);

// Boos the far by adding the far with the fooed far.
float boo(float far);

int truly(int bad); // This is an awful function
